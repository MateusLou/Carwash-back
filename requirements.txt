# Core Framework
fastapi==0.111.0
uvicorn==0.29.0
python-multipart==0.0.9

# Database & ORM
# 2.0.36 (e não a 2.0.28 do template): até a 2.0.30 o SQLAlchemy quebra no
# import sob Python 3.13, por causa dos atributos novos que a 3.13 põe nas
# classes (__static_attributes__, __firstlineno__).
SQLAlchemy==2.0.36
alembic==1.13.1
# 2.9.10 (e não o 2.9.9 do template) porque só a partir dela existe wheel para
# Python 3.13 — sem isso o pip tenta compilar do zero e quebra.
psycopg2-binary==2.9.10

# Authentication & Security
PyJWT==2.8.0
bcrypt==4.1.3
python-dotenv==1.0.1

# Email
sendgrid==6.11.0

# Data Validation
# Mesmo motivo do psycopg2: o pydantic-core do 2.7.1 não tem wheel para 3.13.
pydantic==2.10.6
email-validator==2.1.1

# Dependencies
# 4.12.2 é o mínimo que o pydantic 2.10 exige.
typing-extensions==4.12.2
